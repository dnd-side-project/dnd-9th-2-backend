DROP TABLE IF EXISTS sample;
CREATE TABLE sample(
                                     `sample_id` BIGINT NOT NULL AUTO_INCREMENT,
                                     `data` VARCHAR(255),
                                     PRIMARY KEY (`sample_id`));
